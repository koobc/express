extends layout

block content
  h1= title
  if show
    form(method='post')
      p Jaka jest twoja ulubiona pora roku?
      ul  
        each item in data
          li
            input(type='radio' name='quiz' value=item._id)
            label=item.title
      input(type='submit' value='Zagłosuj')
      p Aby zobaczyć wyniki oddaj głos
  else
      h1 Dokonałeś już oddanie głosu w ankiecie!
      p Wyniki
      ul
      each item in data
        li #{item.title} głosów: (#{Math.round(item.vote/sum*100)}%)
      p Głosów oddano #{sum}